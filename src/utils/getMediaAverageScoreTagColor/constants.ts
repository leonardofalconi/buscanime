import { Theme } from '../../theme'
import { TGetMediaAverageScoreTagColorMap } from './types'

export const MEDIA_AVERAGE_SCORE_TAG_COLOR_MAP: TGetMediaAverageScoreTagColorMap = {
  high: Theme.colors.persianGreen,
  medium: Theme.colors.selectiveYellow,
  small: Theme.colors.crayola,
}
